function myFunction() {
    var sentence = "I am Learning"; //first variable
    sentence += " a lot from this book!"; //added to the above using operators to concatenate the strings
    document.getElementById("Concatenate").innerHTML = sentence; 
}

